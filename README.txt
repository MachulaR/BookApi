Simple app, using GoogleBookApi (https://developers.google.com/books/docs/v1/using#WorkingVolumes)

App search book only by title. 
Each item show (if exist): cover, title and description. 
Description is shortened to several words. 
When the user reach the end of the page, automatically loads the next page of results (if they exist)